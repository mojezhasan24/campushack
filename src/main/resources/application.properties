# Server Port
server.port=8080

# Active Profile (Defaults to dev, override with SPRING_PROFILES_ACTIVE in prod)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Spring MVC View Resolution (JSP Engine)
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Spring Mail SMTP Settings (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=campushack67@gmail.com
spring.mail.password=${EMAIL_PASSWORD:sjfvvrjzdmcfnmxh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# File Upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
